export function sum(a, b) {
    console.log('The input values to the sum function: ' + a + ', ' + b);
    return a + b;
}

export function multiply(a, b) {
    console.log('The input values to the sum function: ' + a + ', ' + b);
    return a * b;
}

